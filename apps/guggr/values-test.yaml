rabbitmq:
  replicaCount: 1
  managementPlugin:
    enabled: true
  peerDiscoveryK8sPlugin:
    enabled: true

apiService:
  ingress:
    enabled: true
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-dns
    hosts:
      - host: guggr-test.k8s.p5p.eu
        paths:
          - path: /api
            pathType: Prefix
            pathType: Prefix
    tls:
      - hosts:
        - guggr-test.k8s.p5p.eu
        secretName: nginx-tls

frontend:
  ingress:
    enabled: true
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-dns
    hosts:
      - host: guggr-test.k8s.p5p.eu
        paths:
          - path: /
            pathType: Prefix
    tls:
      - hosts:
        - guggr-test.k8s.p5p.eu
        secretName: nginx-tls

agent:
  logLevel: debug

evaluator:
  logLevel: debug
